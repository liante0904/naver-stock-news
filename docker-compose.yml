services:
  news-bot:
    build: .
    image: naver-stock-news-bot:latest
    container_name: naver-stock-news-bot
    user: "1001:1001"  # 잠시 주석 처리
    restart: always
    env_file: .env
    volumes:
      - ~/sqlite3:/app/db
      - ~/log:/app/log
